# 📖 Посібник Користувача Kovcheg

Детальна інструкція для роботи з системою: від завантаження знань до спілкування з AI.

## 🔄 Життєвий Цикл Даних

Робота в Kovcheg складається з трьох основних етапів:

**1. Ingestion (Завантаження)**
Ви кладете файли у папку `sources/` — тут зберігаються всі ваші документи.

**2. Build (Обробка)**
Система читає файли, розпізнає текст (включаючи зображення за допомогою OCR), створює векторні представлення та упаковує все у файл `.ark.json`.

**3. Use (Використання)**
Ви відкриваєте веб-інтерфейс або використовуєте CLI для спілкування з базою знань через питання природною мовою.

---

## 📚 Створення Бази Знань

### 1. Підготовка Файлів

Помістіть ваші документи у папку `sources` у корені проєкту. Підтримуються формати:

```bash
# Копіювання PDF-файлів
cp ~/MyDocuments/*.pdf ./sources/

# Копіювання Word-документів
cp ~/MyDocuments/*.docx ./sources/

# Копіювання таблиць
cp ~/MyDocuments/*.xlsx ./sources/

# Копіювання зображень (будуть оброблені через OCR)
cp ~/MyDocuments/*.jpg ./sources/
```

### 2. Збірка Модуля Знань

Запустіть команду `build`. Система обробить всі файли та створить архів:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main build \
  /app/sources \
  --output /app/data/knowledge.ark.json \
  --title "My Knowledge Base"
```

**Параметри:**
- `/app/sources` — папка з вихідними документами
- `--output` — де зберегти готовий модуль
- `--title` — назва модуля (буде видна у веб-інтерфейсі)

**Результат:** Файл `knowledge.ark.json` з'явиться у папці `data/`. Це криптографічно підписаний контейнер, що містить весь текст, вектори та метадані.

### 3. Час Обробки

Час залежить від розміру документів:
- Малі файли (до 10 MB) — 1-2 хвилини
- Середні файли (10-100 MB) — 5-10 хвилин
- Великі файли (100+ MB) — 15-30 хвилин

Під час обробки система виводить прогрес у консоль.

---

## 🌐 Веб-Інтерфейс

Це основний спосіб взаємодії з системою для більшості користувачів.

### Запуск

```bash
docker compose run --rm --service-ports kovcheg python -m ark_engine.cli.main web
```

Сервер стартує на **http://localhost:8000**

### Використання

**Крок 1: Вибір модуля**
Відкрийте браузер. У лівій панелі побачите список доступних модулів (наприклад, "My Knowledge Base"). Натисніть на нього для активації.

**Крок 2: Питання та Відповіді**
Введіть своє питання у текстовому полі чату. Система виконає пошук релевантних фрагментів із ваших документів та згенерує відповідь за допомогою LLM.

**Крок 3: Цитування (Citations)**
Під кожною відповіддю система показує джерела — конкретні уривки з ваших файлів, на яких базується відповідь. Ви можете натиснути на них для перегляду контексту.

### Приклади Питань

```
"Які основні концепції системи?"
"Напиши резюме першого розділу"
"Які рекомендації щодо налаштування?"
"Порівняй два підходи, описані у документах"
```

---

## 🏪 Менеджер Модулів (Ark Store)

Kovcheg має внутрішне сховище (Store) для постійного зберігання та управління модулями.

### Встановлення Модуля

Переміщує файл з папки `data/` у захищене сховище:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store install \
  /app/data/knowledge.ark.json \
  --allow-untrusted
```

**Параметр `--allow-untrusted`:** Дозволяє встановлювати непідписані модулі (див. розділ про безпеку нижче).

### Перегляд Встановлених Модулів

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store list
```

Виводить:
- ID модуля
- Назву
- Дату створення
- Статус підпису

### Видалення Модуля

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store remove <MODULE_ID>
```

Замініть `<MODULE_ID>` на ID з лісту (наприклад, `mod_abc123`).

---

## 🔐 Безпека та Цифрові Підписи

У критичних середовищах (військові, корпоративні, медичні) важливо знати, що дані не були змінені або підроблені. Kovcheg використовує криптографічні підписи Ed25519.

### 1. Генерація Ключів (для видавців)

Якщо ви хочете створювати та розповсюджувати модулі, створіть пару ключів:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main keygen my_organization
```

Результат: Два файли
- `my_organization.pem` — приватний ключ (зберігайте в безпеці!)
- `my_organization.pub` — публічний ключ (можна ділитися)

### 2. Підпис Модуля

Підпишіть модуль приватним ключем:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main sign \
  /app/data/knowledge.ark.json \
  --publisher my_organization
```

Модуль стає частиною вашої ліцензії та може бути розповсюджений.

### 3. Верифікація (Перевірка Цілісності)

Будь-хто може перевірити, чи файл не був пошкоджений:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main verify \
  /app/data/knowledge.ark.json
```

Виводить:
- Ім'я видавця
- Дату підпису
- Статус валідності

### 4. Управління Довіреними Видавцями

Щоб встановити модуль без прапорця `--allow-untrusted`, видавець має бути у вашому списку:

```bash
# Переглянути довірених видавців
docker compose run --rm kovcheg python -m ark_engine.cli.main trust list

# Додати нового видавця
docker compose run --rm kovcheg python -m ark_engine.cli.main trust add <public_key.pub>

# Видалити видавця зі списку
docker compose run --rm kovcheg python -m ark_engine.cli.main trust remove <publisher_id>
```

---

## 💻 CLI Команди (Командний Рядок)

Для складніших задач можна використовувати CLI безпосередньо.

### Поставити RAG-Запит

Швидко дістаньте відповідь без веб-інтерфейсу:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main ask \
  "Яка основна перевага системи?" \
  --module /app/data/knowledge.ark.json
```

**Параметри:**
- Перший аргумент — ваше питання
- `--module` — шлях до модуля

### Пошук Фрагментів

Знайдіть конкретні фрагменти у базі знань без генерації відповіді:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main search \
  "ключові слова" \
  --module /app/data/knowledge.ark.json \
  --top-k 5
```

`--top-k` — кількість найбільш релевантних результатів (за замовчуванням: 5).

---

## 📋 Підтримувані Формати Файлів

Система автоматично розпізнає та обробляє:

**📄 Офісні Документи та Книги**
- PDF (`.pdf`) — витягує текстовий шар
- Word (`.docx`) — зберігає структуру, заголовки, таблиці
- PowerPoint (`.pptx`) — текст зі слайдів та нотаток
- E-books (`.epub`) — розбиває на розділи

**📊 Таблиці та Дані**
- Excel (`.xlsx`, `.xls`) — малі таблиці конвертуються у Markdown, великі у JSON
- CSV (`.csv`) — обробляється як структуровані дані

**🖼️ Зображення (OCR)**
- JPG, JPEG (`.jpg`, `.jpeg`)
- PNG (`.png`)
- WebP (`.webp`)
- TIFF, BMP (`.tiff`, `.bmp`)

Система використовує Tesseract OCR з підтримкою української мови, щоб розпізнавати текст із фотографій, сканів документів та скріншотів.

**🌐 Веб та Код**
- HTML (`.html`, `.htm`) — очищає від тегів, скриптів та реклами
- Текст та Код: `.txt`, `.md`, `.json`, `.xml`, `.py`, `.js` та інші

---

## 🎯 Практичні Приклади

### Приклад 1: Обробка Наукових Статей

```bash
# 1. Скопіюйте PDF-файли зі статтями
cp ~/Papers/*.pdf ./sources/

# 2. Збудуйте модуль
docker compose run --rm kovcheg python -m ark_engine.cli.main build \
  /app/sources \
  --output /app/data/research.ark.json \
  --title "Research Papers"

# 3. Запустіть веб-інтерфейс
docker compose run --rm --service-ports kovcheg python -m ark_engine.cli.main web

# 4. У браузері задавайте питання на кшталт:
# "Які основні выводи статей?"
# "Порівняй методологію в різних статтях"
```

### Приклад 2: Обробка Корпоративної Документації

```bash
# 1. Додайте документи (DOCX, XLSX, PDF)
cp ~/CompanyDocs/* ./sources/

# 2. Збудуйте підписаний модуль
docker compose run --rm kovcheg python -m ark_engine.cli.main build \
  /app/sources \
  --output /app/data/company.ark.json \
  --title "Company Knowledge"

# 3. Підпишіть його
docker compose run --rm kovcheg python -m ark_engine.cli.main sign \
  /app/data/company.ark.json \
  --publisher company_internal

# 4. Встановіть у Store
docker compose run --rm kovcheg python -m ark_engine.cli.main store install \
  /app/data/company.ark.json
```

### Приклад 3: Обробка Змішаних Медіа (PDF + Зображення + Таблиці)

```bash
# 1. Помістіть різні формати в одну папку
cp ~/Documents/*.pdf ./sources/
cp ~/Documents/*.xlsx ./sources/
cp ~/Photos/instruction_*.jpg ./sources/

# 2. Система автоматично розпізнає кожен тип
docker compose run --rm kovcheg python -m ark_engine.cli.main build \
  /app/sources \
  --output /app/data/mixed_media.ark.json \
  --title "Mixed Media Base"

# 3. Результат: єдина база знань, де текст із зображень розпізнаний, таблиці структуровані, а PDF витягнуті
```

---

## 🔧 Розв'язання Проблем

### Проблема: Модуль не з'являється у веб-інтерфейсі

**Рішення:** Переконайтеся, що файл було успішно збудовано:

```bash
ls -lh data/
# Має показати ваш .ark.json файл
```

Якщо файлу немає, запустіть команду `build` ще раз.

### Проблема: OCR не розпізнав текст із зображень

**Рішення:** Перевірте якість зображення (мінімум 300 DPI). Спробуйте іншу папку або переконайтеся, що текст чіткий.

### Проблема: Таблиця обробилась неправильно

**Рішення:** Excel таблиці розмірністю >50 рядків конвертуються у JSON. Перевірте результат у `data/knowledge.ark.json`.

### Проблема: Помилка при підписуванні модуля

**Рішення:** Переконайтеся, що ви створили ключі командою `keygen`:

```bash
ls -la *.pem *.pub
```

---

## 📞 Потрібна Допомога?

- 📖 [README.md](README.md) — основна документація
- 🔧 [INSTALL.md](INSTALL.md) — інсталяція та налаштування
- 🐙 [GitHub Issues](https://github.com/sashapiscivca-svg/kovcheg/issues) — доповідайте про проблеми
- 👤 [GitHub Автор](https://github.com/sashapiscivca-svg) — контакти

---

**Версія:** Kovcheg Ark Engine v1.0 (MVP)

**Останнє оновлення:** 2025-01-22
