# 📦 Kovcheg Ark Engine: Офлайн Система Управління Знаннями (RAG)

Kovcheg — це автономна, модульна система для управління та пошуку інформації у великих гетерогенних наборах даних (PDF, DOCX, текст), оптимізована для використання LLM в офлайн-середовищах.

Формат `.ark` є криптографічно підписаним контейнером, що зберігає текст, вектори та метадані в одному файлі.

## 🌟 Статус: MVP Complete (Store + Web UI)

| Компонент | Статус |
| :--- | :--- |
| **Ark Format Spec** | v1.0 Фіналізовано |
| **Ark Store CLI** | Працює (`ark store install/list`) |
| **Ark Engine (RAG)** | Працює (Векторний пошук) |
| **Web UI** | Функціонально (Vanilla JS) |
| **LLM Integration** | (Потрібна інтеграція реального офлайн LLM) |

---

## 🚀 Швидкий Старт (Docker)

Для запуску потрібен встановлений Docker та Docker Compose.

### 1. Клонуйте репозиторій та перейдіть у теку
```bash
git clone https://github.com/sashapiscivca-svg/kovcheg.git
cd kovcheg
```

### 2. Створіть тестові директорії
```bash
mkdir -p data/raw sources
echo "Ласкаво просимо до Ковчега. Це офлайн система RAG." > sources/welcome.txt
```

### 3. Зберіть та запустіть сервіси
```bash
docker compose up -d --build
```

### 4. Створіть модуль знань
```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main build /app/sources --output /app/data/onboarding.ark.json --title "Onboarding Guide"
```

### 5. Запустіть Web UI
```bash
docker compose run --rm --service-ports kovcheg python -m ark_engine.cli.main web
```

*Веб-інтерфейс буде доступний за адресою: `http://localhost:8000`*

---

## 💻 CLI Приклади

### Встановлення модуля в локальне сховище
```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store install /app/data/onboarding.ark.json
```

### Перегляд встановлених пакетів
```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store list
```

### Постановка RAG-запиту
```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main ask "Яка основна перевага системи?" --module /app/data/onboarding.ark.json
```

---

## 📁 Структура Проєкту

```
kovcheg/
├── ark_engine/           # Основна логіка системи
│   ├── builders/         # Побудовники контейнерів
│   ├── stores/           # Зберігання модулів
│   ├── search/           # Векторний пошук
│   ├── cli/              # Інтерфейс командного рядка
│   └── tests/            # Модульні тести
├── web_ui/               # Веб-інтерфейс
│   ├── backend/          # FastAPI сервер
│   └── frontend/         # HTML/CSS/JS
├── data/                 # Генеровані .ark файли
├── sources/              # Вихідні документи
├── docker-compose.yml    # Конфігурація Docker
├── Dockerfile            # Образ контейнера
└── README.md             # Цей файл
```

---

## 🔧 Налаштування

### Переменні Оточення
Встановіть у файлі `.env` (якщо потрібно):
```env
ARK_MODULES_PATH=/app/data
API_PORT=8000
API_HOST=0.0.0.0
```

### Docker Volume
Все збережене у папці `data/` автоматично синхронізується з Docker контейнером.

---

## 🧪 Запуск Тестів
```bash
docker compose run --rm kovcheg pytest ark_engine/tests/
```

---

## 📝 Формат Ark
Файл `.ark` — це JSON-контейнер з такою структурою:
```json
{
  "metadata": {
    "title": "Module Title",
    "created_at": "2025-01-15T10:00:00Z"
  },
  "documents": [
    {
      "id": "doc_1",
      "content": "...",
      "chunks": [...]
    }
  ],
  "embeddings": [...]
}
```

---

## 🤝 Внески
Приймаються pull requests. Для великих змін, будь ласка, спочатку відкрийте issue для обговорення змін.

---

## 📄 Ліцензія
Apache v2.0 License — див. файл `LICENSE`

---

## 📞 Контакти
- **GitHub**: [sashapiscivca-svg](https://github.com/sashapiscivca-svg)
- **Проєкт**: [Kovcheg](https://github.com/sashapiscivca-svg/kovcheg)
