# Kovcheg — Офлайн система управління знаннями

Kovcheg — це автономна, модульна платформа для управління та пошуку інформації у великих наборах даних (PDF, DOCX, текст). Система працює повністю офлайн, без залежності від інтернету, та оптимізована для локальних LLM.

Ядро системи — формат **`.ark`**, криптографічно підписаний контейнер, що зберігає текст, вектори та метадані в одному файлі.

## 🌟 Можливості

- **Векторний пошук (RAG)** — пошук релевантної інформації в ваших документах
- **Модульна архітектура** — розділення знань на окремі модулі
- **Офлайн-перший** — працює без інтернету на локальному сервері
- **Криптографічна безпека** — .ark контейнери підписані та захищені
- **Веб-інтерфейс** — просте спілкування через браузер
- **Docker-дружній** — одна команда для запуску

## 🚀 Швидкий Старт

### Вимоги

- Docker та Docker Compose

### 1. Клонування та Підготовка

```bash
git clone https://github.com/sashapiscivca-svg/kovcheg.git
cd kovcheg

# Створіть системні папки
mkdir -p data/raw sources
```

### 2. Додавання Знань

Покладіть документи у папку `sources` (PDF, DOCX, TXT, MD):

```bash
# Приклад:
echo "Ласкаво просимо до Ковчега. Це офлайн система RAG." > sources/welcome.txt

# Або скопіюйте існуючі файли:
cp ~/Documents/*.pdf ./sources/
```

### 3. Збірка та Запуск

```bash
# Зберіть та запустіть сервіси
docker compose up -d --build

# Створіть модуль знань з документів
docker compose run --rm kovcheg python -m ark_engine.cli.main build \
  /app/sources \
  --output /app/data/knowledge.ark.json \
  --title "Knowledge Base"

# Запустіть веб-сервер
docker compose run --rm --service-ports kovcheg python -m ark_engine.cli.main web
```

Веб-інтерфейс буде доступний за адресою: **http://localhost:8000**

### 4. Використання

1. Відкрийте браузер на `http://localhost:8000`
2. У лівій панелі вибіть модуль "Knowledge Base"
3. Задавайте питання — система шукатиме відповіді у ваших документах

## 💻 CLI Команди

### Установка модуля

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store install /app/data/knowledge.ark.json
```

### Список встановлених модулів

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main store list
```

### Пошук в модулі (RAG-запит)

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main ask \
  "Яка основна перевага системи?" \
  --module /app/data/knowledge.ark.json
```

## 📁 Структура Проєкту

```
kovcheg/
├── ark_engine/              # Основна логіка системи
│   ├── builders/            # Побудовники .ark контейнерів
│   ├── stores/              # Зберігання та управління модулями
│   ├── search/              # Векторний пошук та RAG
│   ├── cli/                 # Командний рядок (CLI)
│   └── tests/               # Модульні тести
├── ark_format/              # Специфікація формату .ark
│   ├── spec/                # Документація формату .ark
│   ├── sample_modules/      # Приклади .ark модулів
│   └── validation_scripts/  # Скрипти для валідації контейнерів
├── web_ui/                  # Веб-інтерфейс
│   ├── backend/             # FastAPI сервер
│   └── frontend/            # HTML/CSS/JavaScript
├── data/                    # Згенеровані .ark файли
├── sources/                 # Вихідні документи для обробки
├── docker-compose.yml       # Конфігурація Docker
├── Dockerfile               # Docker образ
├── requirements.txt         # Python залежності
├── download_model.py        # Скрипт для завантаження LLM
├── LICENSE                  # Apache v2.0
└── README.md                # Документація
```

## 🔧 Налаштування

### Змінні оточення

Створіть файл `.env` (опційно):

```
ARK_MODULES_PATH=/app/data
API_PORT=8000
API_HOST=0.0.0.0
```

### Docker Volumes

Папка `data/` автоматично синхронізується з контейнером — всі `.ark` файли зберігаються локально.

## 🧪 Запуск Тестів

```bash
docker compose run --rm kovcheg pytest ark_engine/tests/
```

## 📝 Формат .ark

Файл `.ark` — це JSON-контейнер з такою структурою:

```json
{
  "metadata": {
    "title": "Module Title",
    "created_at": "2025-01-15T10:00:00Z"
  },
  "documents": [
    {
      "id": "doc_1",
      "content": "...",
      "chunks": [...]
    }
  ],
  "embeddings": [...]
}
```

## 📊 Статус Розробки

| Компонент | Статус |
|-----------|--------|
| Ark Format Spec | ✅ v1.0 Фіналізовано |
| Ark Store CLI | ✅ Працює |
| Ark Engine (RAG) | ✅ Працює |
| Web UI | ✅ Функціонально |
| LLM Integration | 🔄 В розробці |

## 🤝 Внески

Приймаються pull requests. Для великих змін спочатку відкрийте issue для обговорення.

## 📄 Ліцензія

Apache v2.0 License — див. файл [LICENSE](LICENSE)

## 📞 Контакти

- **GitHub:** [sashapiscivca-svg](https://github.com/sashapiscivca-svg)
- **Проєкт:** [Kovcheg](https://github.com/sashapiscivca-svg/kovcheg)
