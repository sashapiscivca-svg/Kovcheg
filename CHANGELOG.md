## 📜 Changelog

Усі значні зміни та віхи проєкту **Kovcheg** документуються в цьому файлі.

---

## v0.1.0 — MVP (2025-11-23) 🚀

Перший офіційний реліз, що забезпечує повний, автономний цикл RAG-пошуку та управління знаннями.

### ✨ Новий Функціонал (Innovations)

#### 🧠 Core Engine & RAG
* **Гібридний Пошук (Hybrid Search):** Реалізовано алгоритм зваженого пошуку, що комбінує векторну подібність (Cosine Similarity, вага 0.6) та лексичний збіг ключових слів (Keyword Match, вага 0.4) для підвищення точності релевантності.
* **MonsterLoader™:** Універсальний драйвер імпорту даних, що підтримує:
    * **Документи:** PDF (з відновленням тексту), DOCX (зі збереженням таблиць), PPTX, EPUB.
    * **Таблиці:** XLSX та CSV з автоматичною конвертацією у Markdown або JSON залежно від розміру.
    * **Web:** HTML з очищенням від скриптів та стилів.
* **Вбудований OCR:** Інтеграція з **Tesseract 5** для автоматичного розпізнавання тексту на зображеннях (JPG, PNG, WEBP, TIFF) та сканованих PDF. Підтримка української мови "з коробки".
* **Потокова Генерація (Streaming):** Реалізовано механізм Server-Sent Events (SSE/NDJSON) для миттєвого відображення токенів відповіді LLM у реальному часі.

#### 🛡️ Security & Trust
* **Crypto-підписи Ed25519:** Впроваджено повний цикл підпису модулів `.ark`.
    * Генерація ключів (`keygen`) та створення підписів (`sign`) на еліптичних кривих.
    * Валідація цілісності контенту через SHA-256 хеш-суми.
* **Trust Store:** Система управління довіреними видавцями (`trusted_publishers.json`). CLI дозволяє додавати/видаляти публічні ключі та блокувати встановлення непідписаних модулів.

#### 💻 Web UI & Session Management
* **Динамічні Сесії:** Можливість завантажувати файли прямо в чат (Drag-n-Drop). Файли обробляються "на льоту" і додаються до тимчасового векторного індексу сесії без необхідності повної перезбірки модуля.
* **Цитування Джерел:** Інтерфейс відображає точні фрагменти тексту (chunks) та оцінку релевантності (score), на яких базується відповідь AI.
* **Package Manager UI:** Візуалізація встановлених модулів з метаданими (версія, автор, розмір, статус верифікації).

#### ⚙️ Infrastructure
* **Ark Store CLI:** Команди `install`, `remove`, `list` та `doctor` для управління локальним сховищем пакетів.
* **Optimized Docker Build:** Використання `libopenblas-dev` та спеціальних прапорців компіляції (`-DLLAMA_BLAS=ON`) для прискорення роботи LLM на CPU.
* **Auto-Model Download:** Скрипт ініціалізації автоматично завантажує квантовану модель **Qwen 2.5 3B Instruct** (GGUF).

### 🐛 Виправлення (Fixes)
* Виправлено обробку кодувань для текстових файлів через `chardet`.
* Оптимізовано розбиття тексту (Chunking): додано перекриття (overlap) 100 символів для збереження контексту між фрагментами.

### ⚠️ Відомі Обмеження
* **Multi-Tenant:** Відсутній повноцінний поділ прав доступу користувачів (працює в режимі адміністратора).
* **Remote Sync:** Відсутня вбудована синхронізація модулів по мережі (лише локальні файли).

---

## v0.1.0 — MVP (Previous Tag: 2025-11-20)

Базова версія, що забезпечує мінімально життєздатний продукт.

### ✨ Додано
* **Формат Даних:** Фіналізовано базову специфікацію **`.ark` контейнера**.
* **Інгест (Ingest):** Повний пайплайн для побудови модулів знань:
    * Обробка джерел (PDF/DOCX/TXT).
    * Розбиття на **chunks** та генерація **embeddings**.
* **RAG Engine:** Векторний пошук та адаптер для інтеграції з **Локальною LLM** (використана **Qwen 2.5 7B**).
* **Управління Модулями:** Реалізовано **Ark Store** (`install`, `list`, `remove`).
* **Інфраструктура:**
    * Створено **Веб-інтерфейс** (FastAPI backend + простий frontend).
    * Налаштовано інфраструктуру **Docker Compose** для легкого розгортання.
    * Забезпечено повноцінний **Офлайн режим** роботи.
* **Безпека:** Впроваджено базові **Crypto-підписи** для `.ark` контейнерів.
* **Документація:** Надано повний комплект документації (README, INSTALL, USAGE, ARCHITECTURE).
