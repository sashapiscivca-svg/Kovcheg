# 🔧 Інсталяція Kovcheg

Детальна інструкція для встановлення та запуску системи на вашій машині.

## 📋 Вимоги

Перед початком переконайтеся, що у вас встановлено:

- **Docker** (v20.10+) — [Встановити](https://docs.docker.com/get-docker/)
- **Docker Compose** (v2.0+) — [Встановити](https://docs.docker.com/compose/install/)
- **Git** — для клонування репозиторію
- **~15 GB вільного місця** — для моделі та даних
- **Інтернет-з'єднання** — тільки для першого завантаження моделі

### Перевірка встановлення

```bash
docker --version
docker compose version
git --version
```

## 📥 Крок 1: Клонування Репозиторію

```bash
git clone https://github.com/sashapiscivca-svg/kovcheg.git
cd kovcheg
```

## 📁 Крок 2: Створення Системних Директорій

Создайте папки для даних, вихідних документів та моделі:

```bash
mkdir -p data/raw sources models_cache
```

**Що буде в кожній папці:**
- `data/` — згенеровані `.ark` модулі знань
- `sources/` — ваші вихідні документи (PDF, DOCX, TXT, MD)
- `models_cache/` — закешована AI-модель

## 🤖 Крок 3: Завантаження AI-Моделі

Система використовує **Qwen 2.5 7B Instruct** — компактну та ефективну офлайн-модель.

> ⚠️ **Важливо:** Цей крок потребує інтернету та займе 10-20 хвилин залежно від швидкості з'єднання.

### Автоматичне завантаження (рекомендується)

```bash
python download_model.py
```

Скрипт автоматично завантажить модель у `models_cache/`.

### Ручне завантаження

Якщо скрипт не працює, завантажте вручну:

```bash
wget -O models_cache/qwen2.5-7b-instruct-q4_k_m.gguf \
  https://huggingface.co/bartowski/Qwen2.5-7B-Instruct-GGUF/resolve/main/Qwen2.5-7B-Instruct-Q4_K_M.gguf
```

Або для macOS/Linux без wget:

```bash
curl -L -o models_cache/qwen2.5-7b-instruct-q4_k_m.gguf \
  https://huggingface.co/bartowski/Qwen2.5-7B-Instruct-GGUF/resolve/main/Qwen2.5-7B-Instruct-Q4_K_M.gguf
```

### Перевірка завантаження

```bash
ls -lh models_cache/
# Повинен показати файл розміром ~4.7 GB
```

## 📚 Крок 4: Додавання Ваших Знань

Покладіть документи у папку `sources/`. Підтримуються формати:

- **PDF** — `.pdf`
- **Word** — `.docx`
- **Текст** — `.txt`
- **Markdown** — `.md`

### Приклади

```bash
# Створити тестовий документ
echo "Ковчег — це автономна система збереження знань." > sources/welcome.txt

# Копіювати існуючі файли з комп'ютера
cp ~/Documents/*.pdf ./sources/
cp ~/Downloads/instruction.docx ./sources/

# Скопіювати всі документи з папки
cp -r ~/my_documents/* ./sources/
```

> 💡 **Порада:** Почніть з кількох малих документів для тестування.

## 🏗️ Крок 5: Збірка Docker-Образу

Зберіть контейнер з усіма залежностями:

```bash
docker compose up -d --build
```

Цей процес займе 3-5 хвилин. Docker завантажить базовий образ, встановить Python залежності та скомпілює всі компоненти.

### Перевірка статусу

```bash
docker compose ps
# Повинна показати running сервіс kovcheg
```

## 🧠 Крок 6: Створення Модуля Знань

Обробіть документи зі папки `sources/` та створіть модуль `.ark`:

```bash
docker compose run --rm kovcheg python -m ark_engine.cli.main build \
  /app/sources \
  --output /app/data/knowledge.ark.json \
  --title "Knowledge Base"
```

**Що відбувається:**
1. Система читає всі документи зі `sources/`
2. Розбиває їх на смислові фрагменти (chunks)
3. Створює векторні представлення (embeddings)
4. Зберігає все у файлі `.ark` з метаданими

Час обробки залежить від розміру документів (зазвичай 1-5 хвилин).

### Перевірка результату

```bash
ls -lh data/
# Повинен показати файл knowledge.ark.json
```

## 🚀 Крок 7: Запуск Веб-Сервера

Запустіть веб-інтерфейс:

```bash
docker compose run --rm --service-ports kovcheg python -m ark_engine.cli.main web
```

Система автоматично завантажить створений модуль та запустить сервер.

## 🌐 Крок 8: Використання

Відкрийте браузер:

```
👉 http://localhost:8000
```

**Що ви побачите:**

1. **Ліва панель** — список доступних модулів ("Knowledge Base")
2. **Головна область** — чат для спілкування
3. **Праворуч** — результати пошуку з вашими документами

Задавайте питання — система шукатиме відповіді у ваших документах та генеруватиме результати за допомогою LLM.

## 🛑 Зупинення Системи

### Зупинити веб-сервер

```bash
Ctrl + C
```

### Зупинити контейнер

```bash
docker compose down
```

### Повне очищення (видалити контейнер)

```bash
docker compose down -v
```

## ⚙️ Налаштування (Опційно)

### Змінити порт API

Відредагуйте `docker-compose.yml`:

```yaml
ports:
  - "9000:8000"  # Змініть перший номер на потрібний
```

Тоді сервер буде на `http://localhost:9000`

### Змінні оточення

Створіть файл `.env`:

```env
ARK_MODULES_PATH=/app/data
API_PORT=8000
API_HOST=0.0.0.0
```

## 🐛 Розв'язання Проблем

### Проблема: Docker не встановлено

**Рішення:** Встановіть Docker з офіційного сайту https://docker.com/get-started

### Проблема: Недостатньо місця на диску

**Рішення:** Потрібно мінімум 15 GB вільного місця (4.7 GB для моделі + місце для даних)

```bash
# Перевірте вільне місце
df -h
```

### Проблема: Помилка при завантаженні моделі

**Рішення:** Перевірте інтернет-з'єднання та спробуйте ручне завантаження через curl/wget

### Проблема: Помилка `docker compose: command not found`

**Рішення:** Встановіть Docker Compose v2. Для старіших версій замініть `docker compose` на `docker-compose`

### Проблема: Веб-інтерфейс не доступний на localhost:8000

**Рішення:** 
```bash
# Перевірте, чи запущений контейнер
docker compose ps

# Переглядайте логи
docker compose logs kovcheg
```

## 📊 Системні Вимоги (Рекомендовано)

| Компонент | Мінімум | Рекомендовано |
|-----------|---------|---------------|
| **CPU** | 2 ядра | 4+ ядра |
| **RAM** | 4 GB | 8+ GB |
| **SSD** | 20 GB | 50+ GB |
| **Інтернет** | Потрібен для завантаження | Відключається після установки |

## ✅ Контрольний Список

Перед тим як почати, переконайтеся:

- [ ] Docker та Docker Compose встановлені
- [ ] Git встановлений
- [ ] ~15 GB вільного місця
- [ ] Інтернет-з'єднання для завантаження моделі
- [ ] Репозиторій клонований
- [ ] Папки `data`, `sources`, `models_cache` створені

## 🎓 Наступні Кроки

Після успішної інсталяції:

1. Додайте більше документів до `sources/`
2. Пересоздайте модуль знань (крок 6)
3. Спробуйте різні питання в веб-інтерфейсі
4. Дивіться [README.md](README.md) для більш деталей про CLI команди

## 📞 Потрібна Допомога?

- 📖 [README.md](README.md) — основна документація
- 🐙 [GitHub Issues](https://github.com/sashapiscivca-svg/kovcheg/issues) — доповідайте про проблеми
- 👤 [GitHub Автор](https://github.com/sashapiscivca-svg) — контакти

---

**Приємного користування Kovcheg! 🚀**
